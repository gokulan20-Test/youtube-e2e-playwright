# YouTube E2E Test (Playwright)

This project contains an end-to-end (E2E) automated test for YouTube using **Playwright** and **@playwright/test**.  
The test covers search, video playback, pause, seek, screenshot capture, and title verification.

---

## 📦 Prerequisites
- macOS (works on Windows/Linux too)
- Node.js v18 or newer
- npm (comes with Node.js)
- Internet connection

---

## ⚙️ Setup

1. Clone this repository:
   ```bash
   git clone https://github.com/<your-username>/youtube-e2e-playwright.git
   cd youtube-e2e-playwright
